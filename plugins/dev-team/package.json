{
  "name": "dev-team",
  "version": "1.2.0",
  "description": "Dev-team plugin manifest for Oh-My-Pi. Declares the blocking-guard (plan-gate, review-gate, path/freeze/spec guards, serena-forge C# write enforcement) + routing extensions so a marketplace install loads them; agents/skills/commands/.mcp.json are discovered by their conventional sibling directories.",
  "private": true,
  "omp": {
    "extensions": [
      "./extensions/model-routing.ts",
      "./extensions/path-guard.ts",
      "./extensions/destructive-guard.ts",
      "./extensions/serena-enforce.ts",
      "./extensions/serena-build-net.ts",
      "./extensions/careful-mode.ts",
      "./extensions/freeze-guard.ts",
      "./extensions/spec-guard.ts",
      "./extensions/plan-gate.ts",
      "./extensions/review-gate.ts",
      "./extensions/impl-verify.ts",
      "./extensions/telemetry.ts",
      "./extensions/canary.ts",
      "./extensions/domain-scope.ts"
    ]
  }
}
