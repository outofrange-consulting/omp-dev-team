# /test-improve executive summary — {{repo_slug}} ({{run_date}})

> Generated by `/test-improve` Phase 9 from the git-tracked `.dev-team-reports/test-improve/{{slug}}/data/` directory (see §3) plus the process/audit state under `.claude/memory/test-improve/{{slug}}/`. Regeneratable: delete this file and re-run Phase 9 against those persisted artifacts to reproduce it byte-for-byte.

## 1. Bottom line

{{bottom_line_narrative}}

| Metric | Baseline | Achieved | Δ |
| --- | --- | --- | --- |
| Line coverage | {{baseline_line_cov}} | {{achieved_line_cov}} | {{delta_line_cov}} |
| Branch coverage | {{baseline_branch_cov}} | {{achieved_branch_cov}} | {{delta_branch_cov}} |
| Mutation score (hard kills) | {{baseline_mutation_hard}} | {{achieved_mutation_hard}} | {{delta_mutation_hard}} |
| Mutation timeouts (reported separately) | {{baseline_mutation_timeouts}} | {{achieved_mutation_timeouts}} | {{delta_mutation_timeouts}} |
| Determinism | {{baseline_determinism}} | {{achieved_determinism}} | {{delta_determinism}} |
| Wall-clock (full suite) | {{baseline_wallclock}} | {{achieved_wallclock}} | {{delta_wallclock}} |

_When mutation was disabled at Phase 0 (mode `off`), the mutation rows read **"Not applicable — mutation disabled at Phase 0."**_

_When mutation mode was `kill-loop` (mutant-kill loop with no Phase-2 baseline), the Baseline and Δ cells read **"Not applicable — no baseline run (kill-loop mode)."** and Achieved carries the final surviving-mutant count. Only `baseline+kill-loop` populates the baseline and Δ cells._

_When the stack is Go, the mutation row carries the footnote: **"Advisory only — go-mutesting is alpha; survivor count is not a gate for this run."**_

### Tests by type

| Type | Baseline | Achieved | Δ |
| --- | --- | --- | --- |
| Static analysis | {{baseline_static_analysis}} | {{achieved_static_analysis}} | {{delta_static_analysis}} |
| Unit | {{baseline_unit}} | {{achieved_unit}} | {{delta_unit}} |
| Component | {{baseline_component}} | {{achieved_component}} | {{delta_component}} |
| Contract | {{baseline_contract}} | {{achieved_contract}} | {{delta_contract}} |
| Integration | {{baseline_integration}} | {{achieved_integration}} | {{delta_integration}} |
| End-to-end | {{baseline_end_to_end}} | {{achieved_end_to_end}} | {{delta_end_to_end}} |

_Counts are test suites/files, not individual test cases. A type with zero
suites still renders its row (`0 / 0 / 0`) — never omitted._

_When `test-counts-after.json` does not exist (the operator quit before
Phase 8 ran), every row's Achieved and Δ cells read **"Not applicable —
Phase 8 not run."**; Baseline still renders from `test-counts-before.json`._

## 2. What was done this run

{{phase_summary}}

- Phase 0 — approach contract resolved
- Phase 1 — analysis via `/test-health`
- Phase 2 — coverage {{mutation_baseline_phrase}} baseline captured
- Phase 3 — {{gherkin_phrase}}
- Phase 4 — {{triage_summary}}
- Phase 5 — improve without refactoring
- Phase 6 — refactor decision: `{{phase_6_choice}}`
- Phase 7 — refactor-for-testability {{phase_7_phrase}}
- Phase 8 — quality targets: {{targets_summary}}

## 3. What was measured

{{measurement_narrative}}

Baseline artifacts (git-tracked under `.dev-team-reports/test-improve/{{slug}}/data/` — copied there unconditionally by Phase 9 from their canonical `.claude/memory/test-improve/{{slug}}/` working copy):

- `baseline-coverage.json`
- `baseline-mutation.json` — _Not applicable in `off` and `kill-loop` modes (no baseline run); advisory-only on Go `baseline+kill-loop`._

Per-Story deltas:

- `coverage-history.json`

## 4. Findings from `/test-health`

{{findings_summary}}

Full analysis: `.claude/memory/test-improve/{{slug}}/phase-1.md`.

## 5. Work completed (Phase 5 — no refactoring)

{{phase_5_completed}}

Review evidence: `.claude/memory/test-improve/{{slug}}/phase-5-review.json`.

## 6. Work completed (Phase 7 — refactor-for-testability)

{{phase_7_completed}}

_When Phase 7 was declined: **"Phase 7 not run — operator chose to backlog REFACTOR_REQUIRED items at Phase 6."**_

Review evidence: `.claude/memory/test-improve/{{slug}}/phase-7-review.json` — _Not applicable when Phase 7 did not run._

## 7. Deferred work — areas requiring refactoring to add tests

{{deferred_summary}}

| Seam needed | Behavior gained | Estimated risk |
| --- | --- | --- |
| {{refactor_backlog_rows}} | | |

_When `refactor-backlog.md` does not exist (no items were backlogged this run), this section reads **"Not applicable — no REFACTOR_REQUIRED items were backlogged this run."** instead of a table._

Backlog file: `.dev-team-reports/test-improve/{{slug}}/refactor-backlog.md` — _Not applicable when no items were backlogged._

## 8. Waivers

{{waivers_summary}}

Waivers file: `.claude/memory/test-improve/{{slug}}/waivers.json` — _Not applicable when no waivers were captured._

## 9. Next actions

{{next_actions}}

## 10. Provenance

- Repository: `{{repo_path}}`
- Slug: `{{slug}}`
- Branch / base SHA: `{{branch}}` / `{{base_sha}}`
- Head SHA: `{{head_sha}}`
- Run started: `{{run_started}}`
- Run completed: `{{run_completed}}`
- Phase-0 settings: `{{phase_0_recap}}`
- Sink: `{{sink_description}}`
- `/test-improve` version: `{{plugin_version}}`

_This report is regeneratable from the git-tracked `.dev-team-reports/test-improve/{{slug}}/data/` directory (with baseline artifacts under it; see §3) plus the process/audit state under `.claude/memory/test-improve/{{slug}}/`. Delete and re-run Phase 9 to reproduce it._
